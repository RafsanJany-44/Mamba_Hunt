# Standalone checkpoints

Place these two compatible checkpoint files here:

- `PURE_cross_RhythmMamba.pth`
- `UBFC_cross_RhythmMamba.pth`

After that, cross-dataset inference does not access the official code folder.
